# Presenter Notes - ASX Instantiation Review

Talking points for presenting `ASX-Instantiation-Findings.pptx` (23 slides) to
the sub-group. **The same talk track is embedded per-slide as speaker notes in
the deck itself** (regenerated by `build_findings_deck.py`); this file is the
section-level view. Every content slide carries a small "Dig deeper" line
naming the doc + log section behind it - point at it whenever someone wants
the detail. The framing throughout: this review *builds on* the proposed
extension and Elizabeth's message work. Everything is a proposal; nothing has
been applied to the model or the workbooks.

## Time budget (30-minute slot)

- Opening: title + the one-slide summary (slides 1-2) - ~3 min
- How it was produced + contributions (3-4) - ~4 min (the dedicated method
  slide was dropped as familiar ground; its one-sentence version is in the
  process slide's notes)
- Where the instantiation stands (5-6) - ~4 min
- Model state + the structural block: P1, P2, P7 (7-11) - ~7 min
- Findings by theme: re-adopt, content types, typing, authority (12-15) - ~6 min
- The decision register + refinements (16-19) - ~4 min
- Close: next steps (20) - ~1 min
- The reading guide (21-23) is handout material - point at it in one sentence.

If the slot is 20 minutes: slides 1-2, 4, 7-11, the decisions grouping (16),
and next steps (20); the rest is handout.

## Opening (slides 1-2)

- "We checked the proposed ASX extension bottom-up: for each scenario we tried
  to write the actual messages it needs, using the proposed elements. Where a
  message won't build cleanly, that's a concrete gap."
- One sentence on scope: converted the sample-message workbooks to a diff-able
  format so instantiations can be reviewed in git; the original `.xlsx` are
  untouched.
- Slide 2 is the whole review in one slide - if discussion takes over later,
  that slide plus the decision register is the minimum to land.

## The process slide (slide 3) - how the work was produced

- Three coordinated sessions plus an independent check: (1) **document mining**
  hunts primary mission documents; (2) **scenario extraction** distills each
  into a standardized record with the locked v2.2 prompt; (3) **ontology
  application** walks each scenario into concrete messages against the proposed
  ASX elements. A (4) **verification pass** then re-checked every RDF-grounded
  claim, the workbook fidelity, and the citations, with corrections logged.
- Key point: the stages iterate (the walk writes the sourcing brief, mining
  answers it, extraction feeds the walk), and nothing rests on an LLM's
  memory - every claim is grounded in the RDF, the workbooks, or a named source,
  and was independently re-verified.

## The contributions slide (slide 4) - what the group is getting

- Five deliverables: ~34 new message instantiations in diff-able workbooks; a
  15-record sourced scenario corpus; a findings log distilled to 24 decisions;
  a finding-by-finding reconciliation with v0.0.3; and the prior-art
  discoveries (BML `WhoMeasuredType` first among them). All proposal-only.
- This slide frames everything after it as "here is where each deliverable
  came from" - useful to keep up while introducing the work.

## State of play + scenarios (slides 5-6) - the numbers are the contribution

- Started from **4** worked instantiations (2 Reports, 2 Orders, **0
  Initialization**) - Elizabeth's baseline. This review drafted **~34 more, to
  ~38** across Init / Order / Report. Slide 5 shows the before/after; slide 6
  ("Scenarios analyzed") shows the 20+ scenarios behind it, closing on the
  evidence pedigree: every gap raised is grounded in a documented mission.
- Initialization went 0 -> 7, Orders 2 -> 17, Reports 2 -> 14. That breadth -
  not any single message - is what surfaced the to-define items.
- Walked scenarios across Init/Order/Report; then integrated a parallel
  scenario-sourcing effort (Cooperative MCM, SubT, sustainment + nine documented
  missions) that grounded every gap in a real mission.
- The one contributed, human-authored scenario (CASEVAC) had no messages; we
  walked it end to end (Init -> Orders -> Reports).

## Where the model stands (slide 7 - the outlook, worth establishing early)

- One consolidated state slide: the OWL (v0.0.1, Jan) and the spreadsheets
  (Jun) evolved separately for ~5 months, so several findings look like drift
  rather than gaps - expected for a v0.0.x model. v0.0.3 (Jul, Michael) shows
  the model moving: first datatype properties (v0.0.1 had none - it was a
  taxonomy of *things*; the layer that says what you *send* hadn't been built),
  the Video Detection Report folded in, an AutonomyLevelCode model.
- The catch-up list is short and known: the attribute layer (Payload, Mobility,
  VehicleType, ...), one autonomy vocabulary (three today), one sensor model
  (three today - that is P2), and two wiring items (the hasAutonomousRoleCode
  range, the circular owl:imports).

## The three foundational decisions (slides 8-11 - the core of the talk)

- **P1 - a UAV/UGV is typed two incompatible ways.** The proposed `Robot` class
  tree runs parallel to the existing SMX `Platform` tree, both under
  `ActorEntity`. Pick `Robot` and you lose the Platform machinery; pick
  `Aircraft`/`Vehicle` and the ASX classes go unused. v0.0.3 declares both
  trees, so the choice is now concretely in the model.
- **P2 - a sensor has no agreed representation.** The same physical device is
  an OWL `Sensor` class, a `SensorType` enum, and `SensorCapability` as
  associated equipment. Initialization forces the choice - an Init message must
  declare what a platform carries; and the same carried-equipment pattern
  returns in weapon / manipulator / cargo typing.
- **P7 - a swarm isn't yet taskable or able to report.** `Swarm` derives from
  `PhysicalEntity` (an inert object), but orders and reports need an
  `ActorEntity`. The fix is already in the base standard: derive from
  `CollectiveEntity`.
- Key point: "all three are typing decisions - so Initialization, not Reports,
  is where the model has to be settled."

## Findings by theme (slides 12-15)

- One slide per theme, all on the same layout (a full-width flagship box, then
  tagged bullets): already-in-the-standard (12), missing content types (13),
  missing typing (14), missing authority semantics (15). Scenarios appear as
  evidence citations on each item, not as headliners - CASEVAC and fire
  support sit alongside MCM, SubT, LIRC, and MUM-T wherever they hit the same
  gap. The former M-series and coverage-recap slides are gone: the pedigree
  moved to slide 6, M1/M2/M5 fold into the content-types items (Q-E/Q-F), and
  M7 + the typos live on Proposed refinements (19).

## Re-adopt, not invent (slide 12 - open the findings on the good news)

- Much of what's "missing" already exists somewhere:
  - The sensor-measurement report is prior art in C2SIM's own **BML** lineage -
    `WhoMeasuredType` (value + unit + phenomenon + sensor + time + place).
  - Rules of engagement, desired effects, routes, resources, maritime vessels,
    and a neutral hostility value (`smx#NEUTRL`) **already exist** in the base
    standard - verified against the RDF, not from memory. (Command
    authorization of an engagement does *not* exist - that is the W1 gap;
    `AuthorizationHeader` is only message-sender authentication.)
  - The **task-verb vocabulary already exists**: LOX has 446 `TaskActionCode`
    verbs (engage, attack, breach, construct, clear, transport, resupply,
    rescue, recover, neutralize, escort, follow, recce, patrol, sweep, deceive).
    So the task/effect work is not "invent verbs" - it is the payload/effector/
    weapon *typing* plus a couple of residual semantics.

## Decisions (slides 16-18 - the ask)

- The three decisions slides carry a subtitle pointing to **log section 7** (all
  24 in full, with what each resolves) - point at the log rather than reading
  them out. The last three slides are the scaffolding: "How to use this
  material" (reading paths by time budget), the finding-ID decoder ring, and
  "Where to dig deeper" (topic -> doc map). Close on those so the group leaves
  knowing exactly where to start.
- ~24 decisions, listed in the log. Present the grouping, not all 24:
  - **Structure first:** UAV/robot typing (Q-A), swarm -> CollectiveEntity (Q-B),
    one sensor model (Q-C), one autonomy vocabulary (Q-D).
  - **Then content types:** generic detection report (re-adopt BML), rationale
    report, robot-to-robot, engagement authority, payload/effector/weapon typing
    (the task verbs already exist in LOX), area as a task/report subject, and
    the rest.

## Close: where the ball is

- Sourcing is essentially complete - every gap this review raised is now grounded
  in a real mission (one nice-to-have left: a dedicated autonomous-CASEVAC
  mission).
- The remaining work - build the attribute layer, reconcile the tracks, make the
  ~24 decisions - is ours. More scenarios won't fill it.

## Framing and provenance (keep claims precise)

- These are decisions the model defers, and instantiation forces them - not
  defects in the model.
- Provenance: the MUTT-derived scenarios are LLM-generated; the CASEVAC
  scenario is human-authored; the sourced missions are from real papers (two
  carry fidelity caveats - EW and the robotic breach - noted in the records).
- If asked whether we changed anything: **no** - `.xml` copies only, `.xlsx`
  untouched, nothing applied to the OWL. All proposals.
